from pieces import board

def Statistics(Opt):
    if Opt == "yes":
        pass
    if Opt == "no":
        pass















# # def analyseCrossesPossible():
# #     #assume the game is still running. 
# #     oPlaces = []
# #     vertical = True
# #     horizontal = True
# #     cross = True
# #     for row in pieces:
# #         for column in pieces[row]:
# #             if pieces[row][column] == 'o':
# #                 k = map(str,[column,row])
# #                 oPlaces.append(list(k))
# #                 print(f"{row}:{column}")

# def PossiblePlaces():
#     emptyPlaces = []
#     for row in pieces:
#         for column in pieces[row]:
#             if pieces[row][column] == ' ':
#                 emptyPlaces.append(pieces[row][column])
#     return len(emptyPlaces)

# print(PossiblePlaces())

# def winningProbability(noCrosses, noPlaces) -> float:
#     pass 